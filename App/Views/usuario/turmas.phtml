<div class="secao" id="secao-usuario-turmas">
    
    <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-2">
        <h2 class="titulo fs-3 m-0" style="color: var(--cor-primaria, #0d6efd);">
            Turmas de: <?= htmlspecialchars($this->view->usuario['nome'] ?? 'Usuário') ?>
        </h2>
        <a href="/usuarios" class="btn btn-outline-secondary d-flex align-items-center gap-2 btn-ajax">
            <i class="bi bi-arrow-left"></i> Voltar
        </a>
    </div>

    <div class="table-responsive rounded shadow-sm border mt-4">
        <table class="table table-hover table-striped align-middle text-center m-0">
            <thead class="table-light">
                <tr>
                    <th>Série</th>
                    <th>Ano</th>
                    <th>Turno</th>
                    <th style="width: 160px;">Ações</th>
                </tr>
            </thead>
            <tbody>
                <?php if (!empty($this->view->turmas)): ?>
                    <?php foreach ($this->view->turmas as $turma): ?>
                        <tr>
                            <td><?= htmlspecialchars($turma['serie']) ?></td>
                            <td><?= htmlspecialchars($turma['ano']) ?></td>
                            <td><?= htmlspecialchars($turma['turno']) ?></td>
                            <td>
                                <a href="/turma/alunos/<?= $turma['id'] ?>" class="btn btn-ajax btn-sm btn-outline-primary d-flex align-items-center gap-1 mx-auto text-decoration-none" style="width: fit-content;">
                                    <i class="bi bi-eye"></i> Alunos
                                </a>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                <?php else: ?>
                    <tr>
                        <td colspan="4" class="text-muted p-4">Nenhuma turma associada a este usuário.</td>
                    </tr>
                <?php endif; ?>
            </tbody>
        </table>
    </div>
</div>