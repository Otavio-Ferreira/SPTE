<div class="secao" id="secao-inicio">

    <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-2">
        <h2 class="titulo fs-3" style="color: var(--cor-primaria, #0d6efd);">Gerenciar Turmas</h2>
    </div>

    <div class="">
        <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <form method="POST" action="/adicionar/turma" class="modal-content">
                    <div class="modal-header">
                        <h1 class="modal-title fs-5" id="exampleModalLabel">Adicionar turma</h1>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body row">

                        <div class="col-md-6 mb-3">
                            <div class="form-floating">
                                <select class="form-select" name="serie" id="selectSerie" required>
                                    <option value="" selected disabled>Selecione</option>

                                    <optgroup label="Ensino Fundamental I">
                                        <option value="1">1º Ano</option>
                                        <option value="2">2º Ano</option>
                                        <option value="3">3º Ano</option>
                                        <option value="3">4º Ano</option>
                                        <option value="3">5º Ano</option>
                                    </optgroup>
                                    <optgroup label="Ensino Fundamental II">
                                        <option value="6">6º Ano</option>
                                        <option value="7">7º Ano</option>
                                        <option value="8">8º Ano</option>
                                        <option value="9">9º Ano</option>
                                    </optgroup>
                                </select>
                                <label for="selectSerie">Série</label>
                            </div>
                        </div>

                        <div class="col-md-6 mb-3">
                            <div class="form-floating">
                                <select class="form-select" name="ano" id="selectAno" required>
                                    <option value="" selected disabled>Selecione</option>
                                    <option value="2024">2024</option>
                                    <option value="2025">2025</option>
                                    <option value="2026">2026</option>
                                </select>
                                <label for="selectAno">Ano</label>
                            </div>
                        </div>

                        <div class="col-md-6 mb-3">
                            <div class="form-floating">
                                <select class="form-select" name="turno" id="selectTurno" required>
                                    <option value="" selected disabled>Selecione</option>
                                    <option value="Manhã">Manhã</option>
                                    <option value="Tarde">Tarde</option>
                                    <option value="Integral">Integral</option>
                                    <option value="Noite">Noite</option>
                                </select>
                                <label for="selectTurno">Turno</label>
                            </div>
                        </div>

                        <div class="col-md-6 mb-3">
                            <div class="form-floating">
                                <select class="form-select" name="id_usuario" id="selectResp" required>
                                    <option value="" selected disabled>Selecione</option>
                                    <?php foreach ($this->view->usuarios as $key => $usuario) : ?>
                                        <option value="<?= $usuario['id'] ?>"><?= $usuario['nome'] ?></option>
                                    <?php endforeach; ?>
                                </select>
                                <label for="selectResp">Responsável</label>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fechar</button>
                        <button class="btn btn-success h-100 rounded-3 shadow-sm" type="submit">
                            Salvar
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="d-flex justify-content-between align-items-center mb-3">
        <h5 class="m-0 text-secondary">Lista de Turmas</h5>

        <div class="d-flex gap-1">
            <form method="GET" action="" style="max-width: 300px;">
                <div class="input-group">
                    <input
                        type="text"
                        name="busca"
                        class="form-control"
                        placeholder="Buscar turma..."
                        value="<?= isset($_GET['busca']) ? htmlspecialchars($_GET['busca']) : '' ?>">

                    <button class="btn btn-outline-secondary" type="submit">
                        <i class="bi bi-search"></i>
                    </button>
                </div>
            </form>

            <button type="button" class="btn text-white" style="background-color: var(--cor-primaria, #0d6efd); border-color: var(--cor-primaria, #0d6efd)" data-bs-toggle="modal" data-bs-target="#exampleModal">
                <i class="bi bi-plus"></i>
                Adicionar
            </button>
        </div>
    </div>

    <div class="table-responsive rounded shadow-sm border">
        <table class="table table-hover table-striped align-middle text-center m-0">
            <thead class="table-light">
                <tr>
                    <th>Série</th>
                    <th>Ano</th>
                    <th>Turno</th>
                    <th style="width: 160px;">Ações</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->view->turmas as $indice => $item) : ?>
                    <tr>
                        <td><?= htmlspecialchars($item['serie']); ?>º</td>
                        <td><?= htmlspecialchars($item['ano']); ?></td>
                        <td><?= htmlspecialchars($item['turno']); ?></td>
                        <td>
                            <a href="/turma/alunos/<?= $item['id']; ?>" class="btn btn-sm text-primary fw-bold d-flex align-items-center justify-content-center gap-1 mx-auto text-decoration-none" style="background-color: rgba(13, 110, 253, 0.1); border-radius: 8px; width: 90px;">
                                <i class="bi bi-eye"></i> Alunos
                            </a>
                        </td>
                    </tr>
                <?php endforeach; ?>

                <?php if (empty($this->view->turmas)): ?>
                    <tr>
                        <td colspan="4" class="text-muted p-4">Nenhuma turma cadastrada no momento.</td>
                    </tr>
                <?php endif; ?>
            </tbody>
        </table>
    </div>
</div>